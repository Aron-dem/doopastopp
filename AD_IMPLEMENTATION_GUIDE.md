# دليل تطبيق الإعلانات في DopaSTop

## نظرة عامة

تم تطبيق نظام إعلانات غير مزعج يحترم تجربة المستخدم ويوازن بين الإيرادات والرضا.

## مكونات الإعلانات

### 1. مكون AdBanner الأساسي

**الموقع:** `src/components/AdBanner.tsx`

**الخصائص:**
- `position`: موضع الإعلان ('top' | 'bottom' | 'side')
- `delay`: التأخير قبل ظهور الإعلان بالميلي ثانية (افتراضي: 3000)
- `dismissible`: إمكانية إغلاق الإعلان (افتراضي: true)

**الميزات:**
- ظهور متأخر (لا يزعج المستخدم الفوري)
- إمكانية الإغلاق بسهولة
- تصاميم متعددة تظهر عشوائياً
- ألوان جذابة وغير مزعجة
- متجاوب مع جميع الأجهزة

## أماكن الإعلانات الموصى بها

### 1. صفحة المستوى (Level Progress Screen)
```tsx
<AdBanner position="bottom" delay={3000} dismissible={true} />
```
- يظهر بعد 3 ثوانٍ من دخول الصفحة
- في أسفل الشاشة
- يمكن إغلاقه

### 2. صفحة خطة اليوم (Plan Day Screen)
```tsx
<AdBanner position="side" delay={5000} dismissible={true} />
```
- يظهر بعد 5 ثوانٍ
- على جانب الشاشة (يمين)
- يمكن إغلاقه

### 3. صفحة النتائج (Results Section)
```tsx
<AdBanner position="bottom" delay={4000} dismissible={true} />
```
- يظهر بعد 4 ثوانٍ من عرض النتائج
- في أسفل الشاشة

## إضافة إعلانات جديدة

### الخطوة 1: تحديث قائمة الإعلانات

في ملف `AdBanner.tsx`، أضف إعلانك إلى مصفوفة `ads`:

```typescript
const ads = [
  {
    title: 'عنوان الإعلان',
    description: 'وصف الإعلان',
    cta: 'نص الزر',
    color: 'from-purple-600 to-blue-600'
  },
  // أضف إعلانك هنا
  {
    title: lang === 'ar' ? 'عنوانك بالعربية' : 'Your Title in English',
    description: lang === 'ar' ? 'وصفك بالعربية' : 'Your Description in English',
    cta: lang === 'ar' ? 'اضغط هنا' : 'Click Here',
    color: 'from-green-600 to-emerald-600'
  }
];
```

### الخطوة 2: استخدام الإعلان

في أي مكون تريد إضافة الإعلان فيه:

```tsx
import AdBanner from './components/AdBanner';

// في JSX
<AdBanner position="bottom" delay={3000} dismissible={true} />
```

## ألوان الإعلانات المتاحة

استخدم أي من هذه الألوان:

```
from-purple-600 to-blue-600      // بنفسجي إلى أزرق
from-green-600 to-emerald-600    // أخضر إلى زمردي
from-pink-600 to-rose-600        // وردي إلى وردي فاتح
from-orange-600 to-amber-600     // برتقالي إلى كهرماني
from-cyan-600 to-blue-600        // سماوي إلى أزرق
from-indigo-600 to-purple-600    // نيلي إلى بنفسجي
```

## أفضل الممارسات

### ✅ افعل:
- استخدم تأخيرات مختلفة (3-5 ثوانٍ) حسب الصفحة
- اسمح بإغلاق الإعلانات بسهولة
- استخدم ألوان جذابة وليست مزعجة
- أضف إعلانات ذات قيمة للمستخدم
- اختبر على أجهزة مختلفة

### ❌ لا تفعل:
- لا تضع إعلانات على صفحة الاختبار (تشتت المستخدم)
- لا تجعل الإعلانات تحجب المحتوى الرئيسي
- لا تستخدم ألوان صارخة جداً
- لا تضع أكثر من إعلان واحد في نفس الوقت
- لا تجعل الإعلان غير قابل للإغلاق

## تخصيص الإعلانات حسب المستوى

يمكنك إنشاء إعلانات مخصصة لكل مستوى:

```tsx
// في LevelProgressScreen.tsx
const getAdByLevel = (level: number) => {
  if (level === 1) {
    return <AdBanner position="bottom" delay={2000} />;
  } else if (level === 5) {
    return <AdBanner position="bottom" delay={5000} />;
  }
  return <AdBanner position="bottom" delay={3000} />;
};
```

## قياس فعالية الإعلانات

### إضافة تتبع (Analytics)

```tsx
const handleAdClick = () => {
  // تسجيل الحدث
  console.log('Ad clicked');
  // إرسال إلى analytics
  // trackEvent('ad_click', { adType: 'level_banner' });
};
```

## الإعلانات المستقبلية

يمكنك إضافة:
- إعلانات فيديو قصيرة
- إعلانات تفاعلية
- إعلانات شخصية بناءً على المستوى
- إعلانات منتجات حقيقية

## استكشاف الأخطاء

### الإعلان لا يظهر
- تحقق من قيمة `delay`
- تأكد من استيراد المكون بشكل صحيح
- تحقق من وجود الإعلانات في مصفوفة `ads`

### الإعلان يحجب المحتوى
- غيّر `position` إلى `side`
- أضف `margin` أو `padding` للمحتوى الرئيسي
- استخدم `z-index` أقل

### الإعلان لا يغلق
- تحقق من أن `dismissible={true}`
- تأكد من أن زر الإغلاق موجود في الكود
- اختبر على متصفح مختلف

## الخلاصة

نظام الإعلانات في DopaSTop مصمم ليكون:
- **غير مزعج**: ظهور متأخر وإمكانية إغلاق
- **جذاب**: تصاميم ملونة وجذابة
- **مرن**: سهل التخصيص والتعديل
- **فعال**: يحقق أهدافك التسويقية بدون إزعاج المستخدم
