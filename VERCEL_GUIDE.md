# دليل الرفع على Vercel والحصول على روابط الإعلانات

لقد قمت بتعديل المشروع وإضافة الميزات المطلوبة:
1. **نظام الإعلانات-الاختبارات**: يظهر سؤال مفاجئ في منتصف الاختبار وقبل السؤال الأخير.
2. **عقوبة التخطي**: إذا ضغط المستخدم على "تخطي الإعلان"، يحصل على نصف الدرجة فقط (2 من 4).
3. **نسبة تعفن الدماغ**: يتم حسابها بدقة وعرضها كنسبة مئوية.
4. **بطاقة النتائج**: تصميم "ظريف" وجذاب مخصص لأخذ لقطة شاشة (Screenshot) ومشاركته.

---

## أولاً: كيفية الرفع على Vercel

1. اذهب إلى [Vercel.com](https://vercel.com) وسجل الدخول بحساب GitHub الخاص بك.
2. اضغط على زر **"Add New"** ثم اختر **"Project"**.
3. ابحث عن المستودع الذي أنشأته: `dopastop-v2`.
4. اضغط على **"Import"**.
5. في إعدادات البناء (Build Settings)، تأكد من التالي:
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
6. اضغط على **"Deploy"**. سيقوم Vercel ببناء الموقع وإعطائك رابطاً مباشراً.

---

## ثانياً: كيف تحصل على روابط الإعلانات؟

بما أنك تريد كتابة الإعلانات بنفسك، يمكنك استخدام روابط لصور حقيقية أو روابط لمواقع تريد الترويج لها. إليك الطريقة:

1. **روابط الصور**:
   - يمكنك رفع صور الإعلانات على مواقع مثل [ImgBB](https://imgbb.com) أو [Cloudinary](https://cloudinary.com).
   - بعد الرفع، انسخ "رابط الصورة المباشر" (ينتهي بـ `.png` أو `.jpg`).
   - ضع هذا الرابط في ملف `src/components/AdBanner.tsx` في مصفوفة `ads`.

2. **روابط الوجهات (CTA Links)**:
   - إذا كان لديك تطبيق على متجر التطبيقات، انسخ رابط المتجر.
   - إذا كنت تروج لقناة يوتيوب أو حساب تيك توك، انسخ رابط الحساب.

3. **تعديل الإعلانات في الكود**:
   افتح ملف `src/components/AdBanner.tsx` وستجد مصفوفة تسمى `ads`. يمكنك تعديلها بهذا الشكل:

```typescript
const ads = [
  {
    title: 'اسم إعلانك هنا',
    description: 'وصف جذاب للإعلان',
    cta: 'اضغط هنا',
    url: 'رابط الموقع أو التطبيق',
    image: 'رابط الصورة المباشر',
    color: 'from-purple-600 to-blue-600' // ألوان التدرج
  },
  // أضف المزيد هنا...
];
```

---

## ملاحظة تقنية
تم رفع الكود بالكامل إلى مستودعك على GitHub:
`https://github.com/Aron-dem/dopastop-v2`
